name: Mevzuat API Test

on:
  schedule:
    - cron: '*/5 * * * *'
  workflow_dispatch:

jobs:
  test-api:
    runs-on: ubuntu-latest
    steps:
      - name: Test Mevzuat API
        run: |
          echo "Testing Mevzuat API..."
          curl -X POST \
            -H "Content-Type: application/json" \
            -d '{"query": "github actions test", "page_size": 5}' \
            https://mevzuat-mcp-2z26.onrender.com/webhook/search
          echo "API test completed"
